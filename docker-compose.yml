version: "3.8"

services:
  piper-tts:
    image: piper-tts:latest
    container_name: piper-tts
    build: .

    ports:
      - "8080:8080"

    devices:
      - /dev/snd:/dev/snd

    group_add:
      - audio

    restart: unless-stopped
